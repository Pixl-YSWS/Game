{
  "name": "game",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently -n CLIENT,SERVER \"vite\" \"bun --watch server/index.ts\"",
    "start:server": "bun --watch server/index.ts",
    "start:client": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "sync-map": "bun scripts/sync-map.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "concurrently": "^10.0.1",
    "typescript": "~6.0.2",
    "vite": "^8.0.12"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "phaser": "^4.1.0",
    "socket.io": "^4.8.3",
    "socket.io-client": "^4.8.3"
  }
}
